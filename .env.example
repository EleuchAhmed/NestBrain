# .env.example — copy to .env and fill in your values

# ── Obsidian ─────────────────────────────────────────────────
# Absolute path to your Obsidian vault folder
OBSIDIAN_VAULT_PATH=/Users/yourname/ObsidianVault

# From Obsidian → Settings → Community Plugins → Local REST API
OBSIDIAN_API_KEY=

# ── Zotero ───────────────────────────────────────────────────
# From https://www.zotero.org/settings/keys → Create new key
ZOTERO_API_KEY=

# The "userID" from: curl -H "Zotero-API-Key: YOUR_KEY" \
#   https://api.zotero.org/keys/current
ZOTERO_LIBRARY_ID=

# Path to Zotero's storage folder (where PDFs live)
# Mac/Linux: ~/Zotero/storage
# Windows:   C:\Users\YOU\Zotero\storage
ZOTERO_STORAGE_PATH=~/Zotero/storage

# Parent Zotero data directory (contains storage/ and zotero.sqlite)
# Example Windows: C:\Users\YOU\Zotero
ZOTERO_DATA_DIR=~/Zotero

# NotebookLM auth cache directory on host.
# Example Windows: C:\Users\YOU\.notebooklm-mcp
NOTEBOOKLM_MCP_PATH=~/.notebooklm-mcp

# Optional default collection label for pipeline notes
COLLECTION_NAME=Research Synthesis

# Optional display target for the Nestbrain desktop profile
# Windows with X server: host.docker.internal:0.0
DISPLAY=host.docker.internal:0.0

# Force Qt to use X11 xcb plugin for host display bridge route
QT_QPA_PLATFORM=xcb

# ── Docker host routing ──────────────────────────────────────
# Leave as-is for Mac/Windows.
# Linux: replace with output of: ip route | awk '/docker0/{print $9}'
ZOTERO_HOST=host.docker.internal
OBSIDIAN_HOST=host.docker.internal
